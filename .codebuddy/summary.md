# Project Summary

## Overview
This project is built using Java and utilizes the Maven framework for dependency management and build automation. The main libraries used in the project are likely to be those included in the `pom.xml` file, which is the standard configuration file for Maven projects. The project appears to follow a typical structure for a Spring Boot application, given the presence of the `BesportyApplication.java` file and the configuration file `application.properties`.

## Purpose
The purpose of the project is not explicitly stated in the provided information, but based on the naming conventions (e.g., "Besporty"), it could be inferred that the project is related to a sports management or sports event application.

## Build and Configuration Files
The following files are relevant for the configuration and building of the project:

- `/pom.xml`
- `/mvnw`
- `/mvnw.cmd`

## Source Files Directory
The source files can be found in the following directory:

- `/src/main/java/it/unicam/besporty`

## Test Files Directory
The test files are located in:

- `/src/test/java/it/unicam/besporty`

## Documentation Files Location
Documentation files are located at:

- `/HELP.md`